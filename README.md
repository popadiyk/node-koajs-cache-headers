node-koajs-cache-headers
===============================
Add cache headers for all server responses via GET method. Cache time must be set in config file

Usage example
=============
```javascript
var app = koa();
require('node-koajs-cache-headers')(app);
```

